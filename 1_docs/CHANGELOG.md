# CHANGELOG

このプロジェクトでのシステム変更履歴を記録します。
AIエージェントは変更を加えるたびに、このファイルに作業内容を追記してください。

## フォーマット

```markdown
### [#XXX] YYYY-MM-DD
- **ステータス**: `[確認待ち]` | `[マージ済/本番反映]` | `[破棄]`
- **概要**: [変更の簡単な説明]
- **目的**: [なぜこの変更を行ったか]
- **主な変更ファイル**: 
  - `path/to/file`
- **関連**: [過去の案件があれば #XXX と記載]
- **備考**: [特記事項、追加された環境変数、ブランチ名など]
```

## 変更履歴

<!-- ここから下に追記していく（最新のものを一番上にする） -->

### [#007] 2026-04-07
- **ステータス**: `[確認待ち]`
- **概要**: 安否訓練（drill）の Slack 通知を本番と同じチャンネルへ送るよう変更し、仙台・震度6想定の訓練用 JMAXML サンプルを追加
- **目的**: 訓練時も `SLACK_PRODUCTION_CHANNEL_ID` で Bot 投稿し、従業員が本番と同じ手順でボタン回答できるようにするため。あわせて訓練シナリオ説明用の XML サンプルを用意するため
- **主な変更ファイル**:
  - `web/src/lib/slack/notify.ts`
  - `web/drill_sendai_shindo6.xml`
- **関連**: なし
- **備考**: ブランチ名 `feature/drill-slack-channel`。`mode === "drill"` かつ `SLACK_BOT_TOKEN` と `SLACK_PRODUCTION_CHANNEL_ID` がある場合は DM より先にチャンネル投稿。訓練でも `SLACK_PRODUCTION_MENTIONS` を本番と同様に適用。Webhook フォールバックは `drill` で `SLACK_PRODUCTION_WEBHOOK_URL` を利用可能に統一

### [#006] 2026-03-26
- **ステータス**: `[確認待ち]`
- **概要**: 災害通知設定画面をYahoo!防災速報風のUIに刷新
- **目的**: 専門知識がなくても直感的に通知のオン/オフや、発動しきい値（震度5弱以上など）を切り替えられるようにするため
- **主な変更ファイル**: 
  - `web/src/app/(portal)/admin/_components/ActivationMenuEditor.tsx`
- **関連**: なし
- **備考**: キーワード入力式から、選択肢（プルダウン形式のモーダル）とトグルスイッチによる操作に変更。

### [#005] 2026-03-26
- **ステータス**: `[確認待ち]`
- **概要**: 津波注意報の発動しきい値を「注意報」以上に引き下げ
- **目的**: 津波注意報でも警戒が必要なため、自動で安否確認（Slack通知）が行われるようにするため
- **主な変更ファイル**: 
  - `1_docs/設定資料/JMA_SOURCES_JSON_v0.1.json`
  - `1_docs/設定資料/発動条件メニュー対応表_ドラフト.md`
- **関連**: なし
- **備考**: これまで「津波警報」以上でしか安否確認が発動しなかった設定を、「津波注意報」でも発動するように変更。

### [#004] 2026-03-25
- **ステータス**: `[確認待ち]`
- **概要**: 避難情報（避難指示、緊急安全確保）の自動通知機能を追加
- **目的**: 登録地点に避難指示や緊急安全確保が出た際に、自動で安否確認を開始するため
- **主な変更ファイル**: 
  - `1_docs/設定資料/JMA_SOURCES_JSON_v0.1.json`
  - `supabase/migrations/20260325000000_add_evacuation_menu.sql`
- **関連**: なし
- **備考**: ブランチ名 `feature/004-evacuation-notification`。気象庁XMLの「避難指示等（VXAS50）」を監視対象とし、登録地点（市区町村）との突合により発動する。

### [#003] 2026-03-02
- **ステータス**: `[マージ済/本番反映]`
- **概要**: ダッシュボードのインシデント一覧でSlack通知が行われたものだけを表示するように修正
- **目的**: 通知対象者がおらずSlackに通知されなかった（裏側でデータだけ作成された）インシデントが画面上に表示されてしまうのを防ぐため
- **主な変更ファイル**: 
  - `web/src/app/(portal)/_components/AdminDashboardView.tsx`
  - `web/src/app/(portal)/_components/UserDashboardView.tsx`
- **関連**: なし
- **備考**: `slack_thread_ts` が `null` ではないものだけをフィルタリングするように `not("slack_thread_ts", "is", null)` 条件を追加

### [#002] 2026-03-02
- **ステータス**: `[マージ済/本番反映]`
- **概要**: Slack安否回答時の処理をバックグラウンド化（`waitUntil`を使用）
- **目的**: ユーザー情報の取得（`users.info` API）など時間のかかる処理により、Slackの3秒タイムアウト制限に引っかかり「！」マーク（エラー）が表示されてしまう問題を防ぐため
- **主な変更ファイル**: 
  - `web/src/app/api/slack/responses/route.ts`
- **関連**: [#001]
- **備考**: ブランチ名 `fix/slack-response-timeout`。`next/server` に代わり `@vercel/functions` から `waitUntil` をインポートし、APIのレスポンス（200 OK）を即座に返すように修正

### [#001] 2026-02-27
- **ステータス**: `[マージ済/本番反映]`
- **概要**: Slack安否回答時のスレッド返信において、回答者の名前をSlack API (`users.info`) から取得した表示名（Display Name）にするよう改修
- **目的**: 内部IDや英数字のユーザー名ではなく、Slack上で設定されている本来の表示名（例: 藤田伸哉）をスレッド返信で分かりやすく表示するため
- **主な変更ファイル**: 
  - `web/src/app/api/slack/responses/route.ts`
- **関連**: なし
- **備考**: ブランチ名 `feature/slack-display-name`。`SLACK_BOT_TOKEN` を使用してAPIリクエストを実行するよう処理を追加。※本番反映時点で一部ユーザーの日本語表示への切り替わりは未検証だが、取得失敗時はID表示にフォールバックするため安全と判断しリリース。
